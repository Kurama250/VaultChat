{
  "name": "vaultchat",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted E2EE messaging — AES-256-GCM, opaque relay, zero-knowledge server",
  "scripts": {
    "postinstall": "prisma generate",
    "start": "prisma db push && node server/index.js",
    "dev": "prisma db push && node --watch server/index.js"
  },
  "dependencies": {
    "@prisma/client": "^6.14.0",
    "express": "^4.21.2",
    "helmet": "^8.1.0",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "prisma": "^6.14.0"
  },
  "license": "GPL-3.0-only"
}